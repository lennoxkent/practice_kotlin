import kotlin.math.floor
import kotlin.Double

class Heap{
  companion object {
    fun constructHeapBottomUp(input: Array<Int>){
      val lastParentIndex: Int = floor(input.size/(2.toDouble())).toInt()

      
      


    }
  }
}
